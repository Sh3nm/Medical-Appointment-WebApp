# Test Database Configuration
# IMPORTANT: This should point to a SEPARATE test database, NOT your production database!
# You can create a new database in Prisma Cloud or use a local PostgreSQL instance

DATABASE_URL="postgresql://test_user:test_password@localhost:5432/medical_appointment_test?sslmode=prefer"

# JWT (can use same secrets for testing)
JWT_SECRET="test-secret-key"
JWT_REFRESH_SECRET="test-refresh-secret-key"
